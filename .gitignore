/.DS_Store
/locales
/npm-debug.log
/data
/public/uploads
/public/apos-minified
/node_modules
# This folder is created on the fly and contains symlinks updated at startup
# (on Windows, files are copied at startup)
/public/modules
# We don't commit CSS, only LESS
/public/css/*.css
/public/css/*.map
# Don't commit masters generated on the fly at startup, these import all the rest
/public/css/master-*.less
/.jshintrc
/public/js/_site-compiled.js
.env
